fastapi>=0.111.0
uvicorn>=0.29.0
aiocoap>=0.4.7
# Wire-schema toolchain: same zcbor version as the firmware's west module
# (modules/lib/zcbor). 0.9.x needs cbor2<6 (API break in 6.0).
zcbor==0.9.1
cbor2<6
