CONFIG_ZTEST=y

# Keep the FSM's transition log visible in test output: a failing case then
# reads as the same "A -> B (reason; after N s)" trace a UART capture gives.
CONFIG_LOG=y
CONFIG_LOG_MODE_IMMEDIATE=y
